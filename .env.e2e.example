# E2E test credentials (PATCH-001).
# Create a dedicated test user in the Supabase project (email confirmed),
# then put these in .env.local (already gitignored) or export them in CI.
# Without them, authenticated e2e suites skip; the smoke suite still runs.
E2E_EMAIL=
E2E_PASSWORD=
